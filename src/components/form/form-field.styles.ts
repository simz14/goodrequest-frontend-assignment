"use client";

import { css } from "styled-components";

export const filledInputSkin = css`
  height: auto;
  min-height: 0;
  padding: 16px;
  border: none;
  background-color: var(--app-surface-tertiary);
  font-size: 16px;
  font-weight: 400;
  line-height: 24px;
  color: var(--app-content-primary);
`;
